{{- $ref := dict "root" . "name" "device-state-service" "component" "device-state" -}}
{{- $app := dict "root" . "name" "device-state-service" "app" .Values.services.deviceState -}}
kind: Service
apiVersion: v1
metadata:
  name: device-state-service
  annotations:
    {{- if and ( not $app.app.service.insecure ) .Values.global.drogueCloud.useServiceCA }}
    service.beta.openshift.io/serving-cert-secret-name: device-state-service
    {{- end }}
  labels:
    {{- include "drogue-cloud-core.labels" $ref | nindent 4 }}

spec:

  selector:
    {{- include "drogue-cloud-common.selectorLabels" $ref | nindent 4 }}

  type: ClusterIP

  ports:
    - name: service
      port: {{ include "drogue-cloud-core.http-service-port" $app }}
      protocol: TCP
      targetPort: service
