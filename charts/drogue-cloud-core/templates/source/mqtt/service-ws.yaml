{{- $ref := dict "root" . "name" "mqtt-endpoint-ws" "component" "endpoints" -}}
{{- $sel := dict "root" . "name" "mqtt-endpoint" "component" "endpoints" -}}
kind: Service
apiVersion: v1
metadata:
  name: mqtt-endpoint-ws
  labels:
    {{- include "drogue-cloud-core.labels" $ref | nindent 4 }}
spec:
  selector:
    {{- include "drogue-cloud-common.selectorLabels" $sel | nindent 4 }}

  {{- if eq .Values.global.cluster "openshift" }}
  type: ClusterIP
  {{- else if has .Values.global.cluster (list "minikube" "kind" ) }}
  type: NodePort
  {{- else }}
  type: LoadBalancer
  {{- end }}

  ports:
    - name: endpoint
      protocol: TCP
      port: {{ if .Values.endpoints.mqttWs.insecure }}8080{{- else -}}8443{{- end }}
      targetPort: endpoint-ws
      {{- if has .Values.global.cluster (list "minikube" "kind" ) }}
      nodePort: {{ .Values.endpoints.mqttWs.ingress.port }}
      {{- end }}