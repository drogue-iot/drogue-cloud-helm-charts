kind: Service
apiVersion: v1
metadata:
  name: http-command
  labels:
    {{- include "drogue-cloud-core.labels" (dict "root" . "name" "http-command" "component" "endpoints") | nindent 4 }}
spec:
  selector:
    {{- include "drogue-cloud-common.selectorLabels" (dict "root" . "name" "http-endpoint" "component" "endpoints") | nindent 4 }}
  ports:
    - name: command
      protocol: TCP
      port: 80
      targetPort: command
