apiVersion: v1
kind: Secret
metadata:
  name: example-kafka-secret
  labels:
    {{- include "drogue-cloud-core.labels" (dict "root" . "name" "example-kafka-secret" "component" "kafka") | nindent 4 }}
data:
  {{- include "drogue-cloud-common.knative-kafka-net-secret-data" (dict "root" . "userName" "example-user") | nindent 2 }}
